#include <stdio.h>
int main(){
    int a=15,b=4;
    int sum,difference,quotient ,remainder,product;
    sum=a+b;
    difference=a-b;
    quotient=a/b;
    remainder=a%b;
product=a * b;
    printf("sum is %d difference is %d  quotient is %d remainder is %d product is %d",sum,difference,quotient,remainder,product);
  
    return 0;

}